const prompt = require("prompt-sync")();

let num = 0;
let number = 0;

while (number >= 0) {
    number = parseInt(prompt("Digite um número positivo (ou um número negativo parar"));
    if (number >= 0) {
        sum += number;
    }
}

console.log("A soma dos números inseridos é:", sum);